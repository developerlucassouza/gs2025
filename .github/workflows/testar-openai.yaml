name: Testar API OpenAI

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Rodar script Node.js
        run: node script.js
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
